Django>=4.2,<5
djangorestframework
django-cors-headers
djangorestframework-simplejwt
python-dotenv
anthropic  # 키워드 후보 생성(generate_keywords) + 런타임 진료과 추론 폴백(classify_symptom_with_ai)
pydantic   # 위 두 곳의 구조화 출력(messages.parse output_format) 스키마
