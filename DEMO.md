# 잇닥 (ITdoc) 데모 시나리오

> 발표 전 체크: 백엔드(`python manage.py runserver`) + 프론트(`npm run dev`) 실행, http://localhost:5173 접속, 위치 권한 **허용** (거부 시 구미시청 기준으로 자동 폴백되므로 데모는 어느 쪽이든 동작)

## 시연 순서 (약 5분)

### 1️⃣ 일반 추천 흐름 — 핵심 가치 (90초)

| 단계 | 입력/행동 | 보여줄 포인트 |
|---|---|---|
| 홈 | "증상을 입력해 보세요" 클릭 | 모바일 UI, 진료과 바로가기, 면책 고지 |
| 증상 입력 | `어제부터 목이 붓고 침 삼킬 때 아파요. 콧물도 나요` | 300자 카운터, 입력 힌트 칩 |
| 추천 결과 | — | **1순위 이비인후과 (점수 6)** + 추천 근거 태그(`콧물`, `침삼킬때`) — "검색이 아니라 근거 있는 추천" 강조 |
| 병원 리스트 | 이비인후과 카드 클릭 | **카카오맵 임베드** + **추천 탑3 금·은·동 메달 마커**, 거리순 정렬, `가까움`/`영업중` 태그, **영업중만 토글** — **실제 심평원 데이터 554곳** |
| 지도 조작 | 줌 아웃 → 줌 인 | **마커 클러스터링** (축소 시 개수 뱃지로 묶임, 클릭하면 확대) · **위치 변경**(프리셋·지도 클릭) |
| 병원 상세 | 마커 인포윈도우 → 상세 보기 | 미니맵, 실제 진료시간(평일·주말), **🧭 앱 내 길찾기** (실도로 경로선 + 거리·소요시간), 전화하기 |

### 2️⃣ 복수 추천 — 오분류 안전장치 (30초)

입력: `아이가 열이 나고 얼굴에 발진이 생겼어요`
→ **1순위 피부과(발진) / 2순위 내과(발열) / 3순위 소아청소년과(아이가)**
멘트: "단정하지 않고 1~3순위 복수 추천 + 각각의 근거를 보여줘서 오분류 리스크에 대응합니다"

### 3️⃣ 응급 강제 분기 — 안전 설계 (45초)

입력: `갑자기 가슴 통증이 있고 숨이 막혀요`
→ 추천을 **건너뛰고** 빨간 응급 화면: 감지 신호(`가슴통증·심혈관계`, `숨이막혀·호흡기계`) + **119 전화하기**
멘트: "응급 키워드 검사가 추천보다 먼저 실행됩니다. `쓰러지셨어요` 같은 존댓말 활용형도 어간 매칭으로 잡아요"

### 4️⃣ AI 추론 — 사전이 못 잡는 표현 구제 (40초) ⭐핵심

입력: `글자가 두 개로 보이고 초점이 안 맞아요`
→ 규칙 사전엔 없는 비정형 표현이라 매칭 0건 → **런타임에 Claude가 진료과를 추론**
→ **안과** + ✨**AI 추론** 배지 + 근거 문장("복시·초점 조절 장애는 눈의 질환…")
멘트: "흔한 증상은 규칙 사전으로 빠르고 싸게 처리하고, **사전이 못 잡는 비정형 표현만 런타임에 Claude가 구제**합니다. 일반 케이스는 LLM을 안 거쳐서 비용·지연을 통제하죠. AI가 응급을 의심하면 119로 2차 분기까지 합니다."

> 폴백(빈손 방지): AI까지 실패(키 미설정·네트워크 장애)하면 "가까운 내과 찾기"로 안전 착지 — 어떤 경우에도 빈 화면이 없음

### 5️⃣ 회원 기능 — 건강 로그 (40초)

로그인 → 증상 검색 → 마이페이지: **검색 이력이 건강 로그로 자동 저장** (진료과·점수·일시), 삭제 가능
멘트: "게스트도 전 기능 사용 가능하고, 로그인하면 이력이 쌓입니다 (JWT)"

---

## 데이터·기술 어필 포인트

- **실데이터**: 심평원 병원정보서비스 API → 구미·김천·칠곡 병·의원 **554곳**, 진료시간 실데이터 236곳 (주말 진료시간 포함, 요일별 영업 판정)
- **Taxonomy 정규화**: 추천 진료과 16개가 심평원 진료과목코드(dgsbjtCd)와 **코드 레벨 1:1** → 추천→병원 연결에 매핑 오류 없음
- **키워드 사전 562개**: 기본 사전 511개 + **Claude 생성→사람 검수(HITL) 채택 51개**. Django Admin에서 비개발자도 운영 가능 (가중치 인라인 수정)
- **규칙+AI 하이브리드**: 규칙 0건일 때만 런타임 Claude 진료과 추론 폴백(근거 문장 포함). 흔한 표현은 LLM 미호출로 비용·지연 통제, 응급 의심 시 119 2차 안전망
- **앱 내 길찾기**: 카카오모빌리티 REST API를 백엔드 프록시로 호출 → 실도로 경로를 지도에 직접 렌더 (외부 앱 이탈 없음, REST 키 비노출)
- **지도 UX**: 마커 클러스터링(NF03) + 추천 탑3 메달 마커 + 위치 직접 설정(IP 오추정 대응)
- **반경 자동 확장**: 외곽(선산 산지)에서 검색해도 3km→최대 20km 자동 확장으로 빈 화면 없음
- **폴백 설계**: 카카오맵 SDK 실패 시 리스트 뷰, 위치 거부·부정확 시 구미시청 기준 + 수동 설정
- **후기·평점**: 회원 후기(1인 1병원 1후기)를 베이즈 평균으로 집계 → 거리 우선을 깨지 않는 보정 계수로 정렬에 반영(`평점높음` 태그)
- **추천 평가 피드백 보정**: 마이페이지에서 추천 결과에 👍/👎 → 매칭 키워드의 보정 점수에 누적(±5 클램프), 다음 추천 순위에 반영 (ML 학습이 아닌 규칙 기반 보정 — 운영자 사전 보존)

## 예상 Q&A

| 질문 | 답변 |
|---|---|
| 길찾기는 어떻게? | 카카오맵 JS SDK엔 길찾기가 없어 **카카오모빌리티 길찾기 REST API**를 백엔드 프록시로 호출, 경로 좌표를 Polyline으로 렌더. 무료 제공은 자동차 경로만 (도보 미지원) |
| 추천이 틀리면? | 1~3순위 복수 추천 + 근거 노출 + "~가능성" 어조 + 전 화면 면책 고지. 응급은 우선 검사로 강제 분기 |
| 형태소 분석은? | 1차는 부분일치 + 동의어 치환 + 어간 키워드 (예: `쓰러지`가 모든 활용형 매칭). **사전이 못 잡는 비정형 표현은 런타임 Claude 추론으로 보완**해 형태소 분석 없이도 커버리지 확보 |
| 왜 추천 전체를 LLM으로 안 하나? | 비용·지연·일관성 때문. 흔한 증상은 규칙 사전이 즉시·무료·결정적으로 처리하고, **사전이 0건일 때만** LLM 호출(haiku) → 비용·지연을 최소화하면서 롱테일을 커버하는 하이브리드 |
| AI가 엉뚱한 진료과를 답하면? | 진료과 16개 목록 안에서만 고르게 강제(목록 밖이면 폐기), 응급 의심 시 119 분기, 호출 실패 시 내과 폴백. AI는 '대체'가 아닌 '추가 안전망' |
| AI 데이터 전처리는? | 키워드 사전을 Claude로 생성 → **사람 검수(HITL)** → 자동 필터(중복·응급 제외, 가중치 1~3 클램프) → 적재. LLM 생성물을 그대로 쓰지 않고 검수 파이프라인을 거침 |
| 점수는 어떻게? | 키워드별 가중치(대표증상 3, 일반 2, 약한신호 1)를 진료과별 합산. AI 추론 결과는 점수 대신 근거 문장 + 확신도(confidence) 노출 |
| 진료시간 출처는? | 심평원 의료기관별상세정보서비스(월요일 기준 대표값). 미신고 기관은 09~18시 기본값 + "방문 전 전화 확인" 고지 |
| 의료법 이슈는? | '진단'이 아닌 '의료 정보 추천' 포지셔닝, 면책 문구 상시 노출 |

## 트러블슈팅 스토리 (발표 양념용 😄)

1. 카카오 새 콘솔은 도메인을 "플랫폼>Web"이 아닌 **"플랫폼 키>JavaScript SDK 도메인"**에 등록해야 함
2. 공공데이터포털 키는 **서비스별 활용신청** + 승인 후 동기화 15~20분
3. data.go.kr이 **Python 기본 User-Agent를 차단** → `Mozilla/5.0` 헤더로 해결
4. 치과의원은 진료과목을 일반 치과(49)가 아닌 **세부과목(50~61)으로 신고** → 보정 후 168곳 확보
5. 상세정보 API 최신 버전은 문서와 달리 **2.8** (2.7은 Forbidden) — 버전 스캔으로 발견
