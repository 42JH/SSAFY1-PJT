# 잇닥(ITdoc) 트러블슈팅 & 해결 기록

> 개발 중 막혔던 지점과 해결 과정. 발표 "배운 점"·Q&A 대비용. (한 줄 요약 → 원인 → 해결)

---

## A. 공공데이터 API (심평원 / E-Gen)

### A-1. 질병정보 API "서버 장애" 오진단 ⭐

- **증상**: `getDissNameCodeList1` 호출 시 `resultCode 99` + `totalCount 0`. 서버 다운으로 판단해 보강 작업이 멈춤.
- **원인**: 서버 장애가 아니라 **필수 파라미터 누락**. 빈 조건으로는 0건을 반환하는 엔드포인트였음.
- **해결**: 원시 응답을 직접 확인(`NORMAL SERVICE`)하고, `sickType=1&medTp=1(양방)&diseaseType=SICK_NM`을 추가 → KCD 질병 2,065건 정상 수집.
- **교훈**: **에러 메시지(99)를 믿지 말고 응답 본문을 직접 까봐라.**

### A-2. data.go.kr이 Python 기본 User-Agent 차단

- **원인**: 게이트웨이가 `python-urllib` 등 기본 UA를 봇으로 차단.
- **해결**: 요청 헤더에 `User-Agent: Mozilla/5.0` 부여.

### A-3. 인증키는 서비스별 활용신청 + 동기화 지연

- 공공데이터포털 키는 **서비스별로 활용신청** 필요, 승인 후 동기화에 15~20분. (계정당 인증키는 공유 — 질병정보는 병원정보 키 재사용)

### A-4. 치과 세부과목 코드 누락

- **원인**: 치과의원이 진료과목을 일반 치과(49)가 아닌 **세부과목(50~61)으로 신고**해 조회에서 빠짐.
- **해결**: 세부과목 코드까지 포함해 역방향 조회 → 168곳 확보.

### A-5. 상세정보 API 버전 불일치

- **원인**: 문서상 버전(2.7)이 Forbidden.
- **해결**: 버전 스캔으로 실제 동작 버전 **2.8** 발견 → 진료시간 실데이터 236곳 확보.

---

## B. AI / 데이터 전처리

### B-1. 백그라운드 작업이 `tee` 때문에 사망 ⭐

- **증상**: `python manage.py disease_to_keywords | tee $TMPDIR/x.log` 실행이 exit 1로 실패, 3,642행에서 중단.
- **원인**: `tee`가 임시경로 권한오류로 exit 1 → 파이프라인 실패로 처리되며 파이썬 본체까지 종료됨.
- **해결**: `tee` 없이 재실행(출력은 별도 캡처). **장기 백그라운드 작업은 파이프 없이 단독 실행.**

### B-2. LLM 생성물을 그대로 믿지 않기 — 교차매칭 전수 감사 ⭐

- **증상**: "눈이 가렵고 분비물이 나와요" → 산부인과(오답).
- **원인**: 기본 사전의 `분비물 → 산부인과`(단독 generic)가 눈/코 분비물 맥락까지 부분일치로 오인.
- **해결**: 사전 5,253개 전체에서 `키워드A ⊂ 키워드B & 진료과 다름` **구조 스캔** → 좁은 장기특화과로 가는 generic만 필터 → 실제 문장 검증 → firing 버그 수정(`분비물→질분비물`, `교정→치아교정`, `흐릿→안과` 제거 등).
- **정책**: 의학적으로 정확한 매핑(류마티스 관절통→내과)은 보존, **사용자가 말하지 않은 진단을 단정하던 라벨만 완화**(정확성·신뢰성 우선).

### B-3. AI 진료과 환각 방지

- LLM이 16개 진료과 목록 밖 이름을 만들면(전처리 14건) **자동 필터로 폐기**. 런타임 폴백도 목록 강제 + 응급 의심 시 119 분기 + 실패 시 내과 폴백.

### B-4. 매칭 엔진의 한국어 조사·활용형 한계

- **현상**: "외음부**가** 가려워요"는 조사 '가' 때문에 `외음부가려움` 복합어가 부분일치 실패 → generic이 이김.
- **대응**: 근본 한계이므로 규칙 0건일 때 **런타임 AI 폴백**이 커버. ("입궤양" 같은 띄어쓰기 없는 복합어도 동일.)

---

## C. 지도 / 위치 (카카오 · GPS)

### C-1. 카카오 콘솔 도메인 등록 위치 함정

- 새 콘솔은 도메인을 "플랫폼>Web"이 아닌 **"플랫폼 키 > JavaScript SDK 도메인"**에 등록해야 지도가 뜸.

### C-2. PC GPS ±25km 오차

- **원인**: PC는 wifi 기반 측위라 위치 오차가 큼.
- **대응**: 모바일 시연 권장 + 수동 위치 설정(프리셋·지도 클릭)으로 보정 + 구미시청 기준 폴백.

### C-3. 지도 축소 시 추천 1·2·3등 메달이 사라짐

- **원인**: 마커 클러스터링이 탑3 메달 마커까지 묶음.
- **해결**: 탑3 마커는 클러스터에서 제외 처리.

### C-4. 길찾기 핀 하드코딩

- **원인**: 출발/도착 핀이 고정 좌표였음.
- **해결**: 선택한 병원 좌표 기준으로 동적화.

### C-5. 길찾기 REST API 분리

- 카카오맵 JS SDK엔 길찾기가 없음 → **카카오모빌리티 길찾기 REST API**를 백엔드 프록시로 호출(REST 키 비노출), 경로 좌표를 Polyline으로 렌더. (무료는 자동차 경로만)

---

## D. 인증 · 배포(터널)

### D-1. JWT 만료(401) 대응

- 액세스 토큰 만료 시 401 → refresh 토큰으로 자동 갱신 처리.

### D-2. 터널 접속 시 "Blocked request. This host is not allowed."

- **원인**: Vite dev 서버가 터널 도메인 Host 헤더를 거부.
- 2**해결**: `vite.config.js`에 `server.host: true`, `allowedHosts: true`.

### D-3. 모바일에서 위치·지도 안 됨 (HTTP)

- **원인**: `navigator.geolocation`은 **HTTPS(보안 컨텍스트)**에서만 동작.
- **해결**: Cloudflare Tunnel로 HTTPS 임시 도메인 제공 + 그 도메인을 카카오 콘솔에 등록.
- **주의**: quick tunnel은 재시작 시 URL이 바뀜 → 끊기면 카카오 재등록 + 재공유 필요.

> 정식 클라우드 배포는 미완(추후 확장) — README "향후 확장" 참고.
