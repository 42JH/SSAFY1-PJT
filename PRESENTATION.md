# 잇닥(ITdoc) 발표 준비

증상을 입력하면 진료과를 추천하고, 가까운 동네 병원을 거리순으로 안내하는 의료 정보 추천 서비스.
한 줄 소개: **"검색이 아니라 근거 있는 진료과 추천 + 동네 병원 길안내"**

> 프로젝트 소개·API: `README.md` / 진행 기록: `SCRUM.md` / 트러블슈팅: `TROUBLESHOOTING.md`

---

## 0. 발표 개요

| 항목    | 내용                                             |
| ----- | ---------------------------------------------- |
| 총 시간  | 약 12분 (발표 6분 + 라이브 시연 5분 + Q&A) — 배정 시간에 맞춰 조정 |
| 형식    | 슬라이드 + 라이브 시연                                  |
| 시연 환경 | **PC** — wifi 측위라 GPS 오차 큼 → 시연 시작 시 위치를 **수동 설정(구미 프리셋·지도 클릭)으로 고정**                           |

### 발표자 역할 분담 (팀에서 채우기)

| 파트        | 담당자 |
| --------- | --- |
| 도입·서비스 소개 |     |
| 라이브 시연    |     |
| 기술·아키텍처   |     |
| 마무리·Q&A   |     |

---

## 1. 슬라이드 구성 (8장 + 라이브 시연)

### S1. 타이틀 (15초)

- 서비스명 "잇닥(ITdoc)" + 한 줄 정의
- "추천은 진단이 아닌 참고용 정보" 면책 톤을 첫 화면부터

### S2. 문제 정의 (45초)

- "어느 과를 가야 하지?" — 증상은 있는데 진료과를 모름
- 포털 검색의 한계: 광고·불안 조장·근거 불명확 / 응급 판단 지연
- → **근거 있는 진료과 추천 + 응급 우선 분기 + 동네 병원 안내**

### S3. 핵심 가치·차별점 (45초)

- **규칙+AI 하이브리드**: 빠른 규칙 사전으로 흔한 증상 처리, 사전이 못 잡는 비정형 표현은 런타임에 Claude가 추론 구제 (커버리지↑·비용↓)
- **근거 제시**: 매칭된 증상 키워드(또는 AI 추론 근거)를 함께 노출
- **오분류 안전장치**: 단정 않고 1~3순위 복수 추천
- **안전 설계**: 응급 키워드는 추천보다 먼저 검사해 119 강제 분기
- **실데이터**: 심평원 구미권 병·의원 + E-Gen 응급실
- **앱 내 완결**: 외부 앱 이탈 없이 지도·길찾기까지

### S4. 라이브 시연 (5분) → 아래 §2

### S5. 시스템 아키텍처 (45초)

- Frontend: Vue 3 + Pinia + Vue Router
- Backend: Django + DRF (추천 엔진·거리 계산·외부 API 프록시)
- 지도: 카카오맵 JS SDK / 길찾기: 카카오모빌리티 REST API(백엔드 프록시로 키 보호)
- 데이터: 심평원 병원정보·상세정보·질병정보, E-Gen 응급실

### S6. 추천 엔진·데이터 (45초) → 근거는 아래 §4

- 입력 정규화(동의어·어간) → 키워드 사전 매칭 → 진료과별 가중치 합산 → 1~3순위
- **AI 폴백(핵심)**: 규칙 0건일 때만 런타임 Claude가 진료과 직접 추론(근거 문장 포함). 흔한 표현은 규칙으로 빠르고 싸게 처리해 LLM 비용·지연 통제, 응급 의심 시 119 2차 안전망
- **AI 데이터 전처리(핵심)**: 심평원 질병정보 API 실데이터(KCD 2,065건)를 Claude로 "환자 구어체 증상표현+진료과"로 전처리 → 자동 필터 → 사람 검수(HITL) → 적재
- **품질 감사**: 5,253개 전수 구조 스캔으로 generic 키워드 교차오염 버그 검출·수정 (정확성 우선)
- 사용자 평가(👍/👎)로 키워드 가중치 피드백 보정 (ML 아닌 규칙 보정, ±5 클램프)
- 데이터 규모: 키워드 사전 **5,253개** / 진료과 16개(HIRA 코드 1:1) / 병·의원·응급실 실데이터

### S7. 트러블슈팅·배운 점 (45초) → 상세 `TROUBLESHOOTING.md`

시간에 맞춰 1~2개만 골라 짧게:

- **질병 API "장애" 오진단**: resultCode 99를 서버 다운으로 봤으나 필수 파라미터 누락 → "에러 메시지 말고 응답을 봐라"
- **AI 생성물 품질 감사**: LLM 전처리 사전을 그대로 신뢰하지 않고 generic 교차오염(분비물→산부인과)을 전수 스캔으로 검출·수정
- (보조) 카카오 콘솔 도메인 위치 함정 / data.go.kr UA 차단

### S8. 마무리 (30초)

- 요약: 근거 있는 추천 + 안전한 응급 분기 + 실데이터 병원 안내
- 향후 계획: 정식 클라우드 배포, 위치 정확도 보강, 진료시간 커버리지 확대, 형태소 분석 고도화
- 면책 메시지로 마무리 ("진단이 아닌 참고용 정보")

---

## 2. 라이브 시연 시나리오 (약 5분)

> 사전(PC 시연): 백엔드(`runserver`) + 프론트(`npm run dev`) 실행 후, **위치를 수동으로 구미 프리셋에 고정**(PC GPS 오차 회피). 거부/오차 시 구미시청 폴백.

### 1️⃣ 일반 추천 흐름 — 핵심 가치 (90초)

| 단계     | 입력/행동                           | 포인트                                                                          |
| ------ | ------------------------------- | ---------------------------------------------------------------------------- |
| 홈      | "증상을 입력해 보세요"                   | 모바일 우선 반응형 UI, 진료과 바로가기, 면책 고지                                               |
| 증상 입력  | `어제부터 목이 붓고 침 삼킬 때 아파요. 콧물도 나요` | 300자 카운터, 입력 힌트 칩                                                            |
| 추천 결과  | —                               | **1순위 이비인후과** + 근거 태그(`콧물`,`침삼킬때`) — "검색이 아니라 근거 있는 추천"                      |
| 병원 리스트 | 이비인후과 카드 클릭                     | 카카오맵 임베드 + **탑3 금·은·동 메달 마커**, 거리순, `가까움`/`영업중` 태그, 영업중 토글 — **실제 심평원 554곳** |
| 지도 조작  | 줌 아웃 → 인                        | **마커 클러스터링** + 위치 변경(프리셋·지도 클릭)                                              |
| 병원 상세  | 인포윈도우 → 상세                      | 미니맵, 실제 진료시간, **🧭 앱 내 길찾기**(실도로 경로선+거리·소요시간), 전화                            |

### 2️⃣ 복수 추천 — 오분류 안전장치 (30초)

입력: `아이가 열이 나고 얼굴에 발진이 생겼어요`
→ 1순위 피부과(발진) / 2순위 내과(발열) / 3순위 소아청소년과(아이가)
멘트: "단정 않고 1~3순위 + 각 근거로 오분류 리스크 대응"

### 3️⃣ 응급 강제 분기 — 안전 설계 (45초)

입력: `갑자기 가슴 통증이 있고 숨이 막혀요`
→ 추천 건너뛰고 빨간 응급 화면: 감지 신호(`가슴 통증·심혈관계`, `숨막힘·호흡기계`) + **119 전화하기**
멘트: "응급 검사가 추천보다 먼저. `쓰러지셨어요` 존댓말 활용형도 어간 매칭으로 잡고, 표시는 깔끔한 증상명(label)으로"

### 4️⃣ AI 추론 — 사전이 못 잡는 표현 구제 (40초) ⭐핵심

입력: `글자가 두 개로 보이고 초점이 안 맞아요`
→ 규칙 0건 → **런타임 Claude 추론** → **안과** + ✨AI 추론 배지 + 근거 문장
멘트: "흔한 증상은 규칙으로 빠르고 싸게, **사전이 못 잡는 비정형 표현만 런타임 Claude가 구제**. 일반 케이스는 LLM 미호출로 비용·지연 통제. AI가 응급 의심 시 119 2차 분기"

> 폴백: AI까지 실패(키 미설정·네트워크)하면 "가까운 내과 찾기"로 안전 착지 — 빈 화면 없음

### 5️⃣ 회원 기능 — 건강 로그 (40초)

로그인 → 검색 → 마이페이지: 검색 이력이 건강 로그로 자동 저장(진료과·점수·일시), 삭제 가능
멘트: "게스트도 전 기능, 로그인하면 이력이 쌓임 (JWT)"

> 시연 실패 대비: 캡처/녹화 백업 준비.

---

## 3. 데이터·기술 어필 포인트

- **실데이터**: 심평원 병원정보 API → 구미·김천·칠곡 병·의원 **554곳**, 진료시간 실데이터 236곳
- **Taxonomy 정규화**: 추천 진료과 16개 = 심평원 진료과목코드(dgsbjtCd) **코드 레벨 1:1** → 매핑 오류 없음
- **AI 데이터 전처리(아래 §4)**: 질병 API 실데이터 → Claude 전처리 → HITL 검수 → 5,253개 사전
- **규칙+AI 하이브리드**: 규칙 0건일 때만 런타임 Claude 추론(근거 문장 포함), 응급 의심 시 119 2차 안전망
- **앱 내 길찾기**: 카카오모빌리티 REST API 백엔드 프록시 → 실도로 경로 직접 렌더(외부 앱 이탈 없음, REST 키 비노출)
- **지도 UX**: 마커 클러스터링 + 탑3 메달 마커 + 위치 직접 설정
- **반경 자동 확장**: 외곽에서도 3km→최대 20km 자동 확장으로 빈 화면 없음
- **후기·평점**: 회원 후기(1인 1병원 1후기)를 베이즈 평균 집계 → 거리 우선을 깨지 않는 보정 계수
- **추천 평가 피드백 보정**: 👍/👎 → 매칭 키워드 보정 점수 누적(±5 클램프) — ML 아닌 규칙 보정

---

## 4. AI 데이터 전처리 — 근거

### 사전 규모·출처

| 출처                                  | 개수        |
| ----------------------------------- | ---------:|
| 기본 사전(수기)                           | 510       |
| LLM 생성→검수 채택(1차, generate_keywords) | 51        |
| **질병 API 전처리→검수 채택(2차)**            | **4,693** |
| **DB 적재 총계(중복 제거)**                 | **5,253** |

### 질병 API 전처리 파이프라인

```
① import_disease     심평원 질병정보(B551182) → 양방 KCD 질병 2,065건
② disease_to_keywords Claude(haiku)로 '질병명 → 환자 구어체 증상표현+진료과' (173배치)
③ 자동 필터          중복·진료과목록밖·응급겹침 제거, 가중치 1~3 클램프
④ 사람 검토(HITL)    가중치 기준 큐레이션 → keywords_approved.csv
⑤ seed_data          기본 사전 + 채택분 병합 적재
```

### 전처리 퍼널 (질병 2,065건 입력)

| 단계           | 수치                                |
| ------------ | ---------------------------------:|
| LLM 생성(raw)  | 5,766                             |
| 자동 필터 탈락     | 589 (중복 555 + 진료과오류 14 + 응급겹침 20) |
| 검수 후보        | 5,177 (통과율 89.8%)                 |
| **사람 검토 채택** | **4,693**                         |

> **"AI가 만들되 사람이 책임진다"**: 진료과 환각 14건 자동 차단, 변별력 없는 약한신호는 사람이 드롭, 5,253개 전수 교차매칭 감사로 generic 오염 버그(분비물→산부인과 등)까지 수정. 의학적 정확성 우선(류마티스 관절통→내과 보존), 과한 진단 단정 라벨만 완화.

> **출처 정정**: 심평원 질병 API는 질병명+KCD코드만 제공(증상 아님). "질병→증상표현+진료과"는 Claude 전처리+사람 검수로 직접 구축.

---

## 5. 추천 정확도 (40케이스 평가)

| 지표                | 건수    | 비율    |
| ----------------- | ----- | ----- |
| Top-1 적중          | 32/40 | 80.0% |
| Top-3 적중          | 36/40 | 90.0% |
| 규칙 커버리지(폴백 아님)    | 38/40 | 95.0% |
| AI 구제 적중(규칙 0건 중) | 2/2   | 100%  |

**오답 4건** (모두 규칙):

- '어지럽고 빙글빙글…' → 정답 이비인후과 / 결과 신경과
- '허리 디스크처럼 다리까지 저려요' → 정답 신경외과 / 결과 정형외과
- '목이 뻐근하고 팔까지 저려요' → 정답 신경외과 / 결과 정형외과
- '예방접종 하러…' → 정답 가정의학과 / 결과 소아청소년과

> 사전 확장 후 재측정 여지: `manage.py eval_recommend --with-ai --md`

---

## 6. 예상 Q&A

| 질문                  | 답변                                                                                    |
| ------------------- | ------------------------------------------------------------------------------------- |
| 길찾기는?               | 카카오맵 JS SDK엔 길찾기 없음 → **카카오모빌리티 REST API** 백엔드 프록시, Polyline 렌더. 무료는 자동차 경로만          |
| 추천이 틀리면?            | 1~3순위 복수 추천 + 근거 + "~가능성" 어조 + 면책 고지. 응급은 우선 검사로 강제 분기                                |
| 형태소 분석은?            | 부분일치+동의어+어간(예: `쓰러지`가 모든 활용형). **비정형 표현은 런타임 Claude 추론으로 보완**                         |
| 왜 전체를 LLM으로 안 하나?   | 비용·지연·일관성. 흔한 증상은 규칙이 즉시·무료·결정적 처리, **0건일 때만** LLM(haiku) — 롱테일 커버 하이브리드              |
| AI가 엉뚱한 진료과?        | 16개 목록 안에서만 강제(밖이면 폐기), 응급 의심 시 119, 실패 시 내과 폴백. AI는 '대체' 아닌 '추가 안전망'                 |
| AI 데이터 전처리는?        | 질병 API 실데이터 → Claude로 증상표현+진료과 전처리 → 자동 필터(환각 14건 차단) → **HITL 검수** → 전수 교차매칭 감사 (§4) |
| AI가 만든 사전, 믿을 수 있나? | "AI가 만들되 사람이 책임진다" — 자동 필터 + HITL + 전수 감사 3단계. 의학적 정확 매핑 보존, 과한 단정 라벨만 완화             |
| 점수는?                | 키워드 가중치(대표 3/일반 2/약한 1) 진료과별 합산. AI 추론은 점수 대신 근거 문장+확신도                               |
| 진료시간 출처는?           | 심평원 상세정보(월요일 대표값). 미신고는 09~18시 기본값 + "방문 전 전화 확인" 고지                                  |
| 위치 부정확하면?           | 거부·오차 시 구미시청 폴백 + 수동 설정(프리셋·지도 클릭). 시연은 PC                                            |
| 회원 안 해도?            | 게스트로 전 기능, 로그인 시 건강 로그 누적                                                             |
| 의료법 이슈?             | '진단' 아닌 '의료 정보 추천' 포지셔닝, 면책 상시 노출                                                     |

---

## 7. 발표 전 체크리스트 & 리스크

### 체크리스트

- [ ] 백엔드(`runserver`) + 프론트(`npm run dev`, :5173) 실행 확인
- [ ] **PC 위치 수동 설정(구미 프리셋·지도 클릭)으로 고정** — wifi 측위 오차 회피 (시연 시작 직후 1회)
- [ ] 카카오맵 타일·마커·길찾기 경로선 렌더 확인
- [ ] 시연 입력 4종 준비(일반/복수/응급/AI) — §2
- [ ] 녹화·스크린샷 백업
- [ ] 5분 시연 타이밍 리허설 1회+, 슬라이드↔시연 전환 점검, 발표자 멘트 확인

### 리스크 & 대응

| 리스크               | 대응                       |
| ----------------- | ------------------------ |
| PC GPS ±25km 오차   | **시작 시 수동 위치 설정(구미 프리셋·지도 클릭)으로 고정** |
| 외부 API(지도·길찾기) 실패 | 리스트 뷰 폴백 + 녹화 백업         |
| 입력 오타·예외          | 검증된 시연 문구 4종 고정          |
| 의료법·정확도 지적        | "진단 아닌 정보 추천" + 면책 상시 노출 |
